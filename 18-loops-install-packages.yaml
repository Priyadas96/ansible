- name: #install packages
  hosts: frontend
  become: yes
  tasks:
    - name: #install different packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: installed
      loop:
        - mysql
        - zip
        - nodejs
