- name: variables in file
  hosts: local
  connection: local
  vars_files:
    - vars_files.yaml
  tasks:
    -name: vars file
    ansible.builtin.debug:
      msg: "Im learning {{ course }} with duration {{ duration }} from {{ Trainer }}"
